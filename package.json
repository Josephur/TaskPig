{
  "name": "taskpig-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "TaskPig monorepo: Tauri app, plugin SDK, and bundled plugins",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@taskpig/plugin-sdk && npm run build --workspace=taskpig",
    "build:sdk": "npm run build --workspace=@taskpig/plugin-sdk",
    "dev": "npm run build --workspace=@taskpig/plugin-sdk && npm run dev --workspace=taskpig",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
